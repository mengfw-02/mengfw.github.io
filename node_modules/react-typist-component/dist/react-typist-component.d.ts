/// <reference types="react" />

import { ReactNode } from 'react';

export declare type Delay = number | (() => number);

export declare type Splitter = (str: string) => string[];

declare const Typist: (({ cursor, disabled, restartKey, children, splitter, typingDelay, backspaceDelay, onTypingDone, startDelay, finishDelay, loop, pause, }: TypistProps) => JSX.Element) & {
    Delay: (props: {
        ms: number;
    }) => null;
    Backspace: (props: {
        count: number;
    }) => null;
    Paste: ({ children }: {
        children: ReactNode;
    }) => JSX.Element;
};
export default Typist;

export declare type TypistProps = {
    children: React.ReactNode;
    typingDelay?: Delay;
    backspaceDelay?: Delay;
    loop?: boolean;
    pause?: boolean;
    startDelay?: number;
    finishDelay?: number;
    onTypingDone?: () => void;
    splitter?: Splitter;
    cursor?: string | React.ReactElement;
    disabled?: boolean;
    restartKey?: any;
};

export { }
